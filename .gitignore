# Virtual environments
env/
.venv/

# Python caches
**/__pycache__/
*.pyc

# Prometheus / Loki / SQLite runtime data
data/
loki-storage/
/tmp/
*.db
*.db-journal
*.db-shm
*.db-wal

# CSV outputs (metrics, sweeps, scored events — all regenerable)
*.csv

# Trained model bundles (regenerable via models/train_*.py)
*.pkl

# Generated synthetic datasets (regenerable via dataset_script/ with fixed
# seeds; also keeps the anchor company log samples out of version control)
Datasets/

# Local contributor guide (machine-specific, not part of the shared repo)
CLAUDE.md

# Claude Code local config (machine-specific; never commit)
.claude/
RUNBOOK.md

